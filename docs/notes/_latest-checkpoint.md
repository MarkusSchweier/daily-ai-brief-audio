# Latest checkpoint

No checkpoint yet. Created automatically before context compaction, or on demand via
`/checkpoint`. Imported by this project's CLAUDE.md so a fresh session can resume in-flight work.
