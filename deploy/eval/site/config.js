// API base URL for the eval review site's fetch target.
//
// Before the eval.mschweier.com custom domain / DNS is wired up, this should point at
// the temporary API Gateway `execute-api` URL from the `HttpApiUrl` CDK stack output
// (e.g. "https://abc123xyz.execute-api.us-east-1.amazonaws.com"). No trailing slash.
window.BRIEF_EVAL_API_BASE_URL = "https://1z23hdhahg.execute-api.us-east-1.amazonaws.com";
