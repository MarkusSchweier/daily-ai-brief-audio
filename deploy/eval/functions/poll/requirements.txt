# poll/handler.py imports httpx (Deployments/Sessions API client) and anthropic
# (the judge LLM's Messages API client, via eval_core/judges/base.py's run_judge()).
# Unlike deploy/managed-agent/microvm/launcher/requirements.txt, this does NOT need
# anthropic's `[webhooks]` extra (no standardwebhooks dependency) -- this function
# never verifies webhook signatures, so every dependency here (anthropic, httpx,
# pydantic, pydantic-core, jiter, distro, sniffio, anyio, certifi, idna,
# typing-extensions) publishes a proper manylinux wheel and survives
# --only-binary=:all: in one single locked pip pass.
anthropic>=0.40.0
httpx>=0.27.0
