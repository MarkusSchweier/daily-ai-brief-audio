{
  "_comment": [
    "Deployments-API payload for the daily AI brief's Managed Agents scheduled deployment.",
    "Source-of-truth per docs/adr/0006 FR-16/AC-15 — this file is what defines the",
    "schedule and which self-hosted environment/agent it targets. It is NOT applied",
    "automatically by this CDK stack; create/update the deployment via the Deployments",
    "API or Claude Console using this file's values (see deploy/managed-agent/README.md).",
    "",
    "Built against beta header: managed-agents-2026-04-01 (PRD FR-2). If Anthropic",
    "changes this API surface, this file's shape may need to change too — the",
    "deployment must fail loudly, not silently skip, if the platform contract changes",
    "(docs/adr/0006, Negative/follow-ups)."
  ],
  "beta_header": "managed-agents-2026-04-01",

  "agent": {
    "_comment": "References the agent definition created via the Claude Console/API (docs/adr/0007). The skill (deploy/managed-agent/skills/daily-ai-brief/) is now a verified verbatim port of the real daily-ai-brief skill, incl. its real sources.md -- not a reconstruction. The skill deliberately does NOT touch Polly/SES/HTML/delivery (matches the real skill's own design: 'converting it to speech and delivering it is the caller's job'). This initial_prompt is that caller/wrapping layer -- it invokes the skill for the writing, then layers the delivery mechanics on top, mirroring how the local Desktop task's SKILL.md wraps the same skill today (its STEP 1 invokes the skill; STEPs 5-8 are the wrapping delivery task).",
    "agent_id": "REPLACE_WITH_AGENT_ID_agent_...",
    "initial_prompt": "1) Invoke the daily-ai-brief skill to research, write, and validate today's brief and produce the listening script, per the skill's own output contract: it writes the brief to /workspace/AI Brief - YYYY-MM-DD.md (today's date) and, since this is always the audio-requested case, save the listening script explicitly to /workspace/listening-script.txt (the skill defers this exact path to the caller). 2) As this deployment's own wrapping task, NOT the skill's job: convert that brief Markdown to clean, inbox-readable HTML and save to /workspace/brief.html. 3) Run the delivery step: `python3.13 /opt/pipeline/audio_email.py`, after exporting BRIEF_MARKDOWN_PATH (the skill's actual dated output file from step 1), BRIEF_HTML_PATH=/workspace/brief.html, LISTENING_SCRIPT_PATH=/workspace/listening-script.txt, MP3_OUT_PATH=/workspace/brief.mp3, EMAIL_SUBJECT='Daily AI Brief — <today, DD.MM.YYYY>', SUBSCRIBERS_TABLE_NAME=brief-subscribers, SUBSCRIBERS_API_BASE_URL=https://2il2bs0iq4.execute-api.us-east-1.amazonaws.com. This synthesizes the narrated MP3 (async Polly, text-only fail-safe on any audio error), sends the owner's copy, fans out to confirmed subscribers (failure-isolated per recipient), and archives the brief + HTML + script to S3 briefs/YYYY-MM-DD/ for tomorrow's run.",
    "skills": [
      "daily-ai-brief"
    ]
  },

  "environment": {
    "_comment": "The self_hosted environment id created via the Claude Console after the CDK stack (ManagedAgentSandboxStack) is deployed and its webhook is registered. See README step 2.",
    "type": "self_hosted",
    "environment_id": "REPLACE_WITH_ENVIRONMENT_ID_env_..."
  },

  "schedule": {
    "_comment": "Matches today's weekday 6:07 AM local run (docs/adr/0006). CONFIRM the owner's actual local timezone before deploy -- America/Los_Angeles is this ADR's assumption, not independently re-verified against the owner's Mac locale.",
    "cron": "7 6 * * 1-5",
    "timezone": "America/Los_Angeles"
  },

  "_manual_steps_not_encoded_here": [
    "Creating the agent (agent_id) and the self_hosted environment (environment_id) via the Claude Console/API.",
    "Registering deploy/managed-agent's WebhookUrl stack output with Anthropic for session.status_run_started.",
    "Generating and populating the environment key + webhook signing secret into the two Secrets Manager secrets this CDK stack creates empty.",
    "Building and pushing the microVM container image (deploy/managed-agent/microvm/) via the reference implementation's build-image.sh-equivalent flow.",
    "Actually calling the Deployments API (or Console) with this file's schedule/agent/environment values to create or update the live scheduled deployment.",
    "See deploy/managed-agent/README.md for the full, ordered runbook."
  ]
}
