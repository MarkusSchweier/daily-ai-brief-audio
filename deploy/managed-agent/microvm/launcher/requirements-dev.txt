-r requirements.txt
pytest>=8.0.0
# moto[dynamodb] backs the idempotency tests (tests/test_idempotency.py) with a
# real (mocked) DynamoDB table, matching deploy/managed-agent/requirements-dev.txt's
# convention elsewhere in this repo.
boto3>=1.34.0
moto[dynamodb]>=5.0.0
