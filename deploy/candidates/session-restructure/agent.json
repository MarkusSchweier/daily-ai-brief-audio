{
  "name": "daily-ai-brief-cost-session-restructure-coordinator",
  "description": "Cost-optimization candidate #4 COORDINATOR: orchestrates the same four-sub-agent decomposition as candidate #3 (research -> selection -> writing -> listening-script), delegating each phase to its sub-agent, but with every sub-agent on Sonnet 5 instead of Haiku -- isolating the structural (no full-context replay) lever from the model lever. Sonnet, orchestration only -- reads recent priors (Step 0), sequences the sub-agents over the shared /workspace working folder, and confirms the four output files. Content generation only; holds no AWS/delivery role. SEPARATE agent resource from the real live production agent (agent_01EswBTose8dnTAUDbGvzdLq) and from every other candidate; syncing/triggering it can never mutate or invoke either.",
  "tools": [
    {
      "type": "agent_toolset_20260401",
      "default_config": {
        "enabled": true,
        "permission_policy": {
          "type": "always_allow"
        }
      },
      "configs": []
    }
  ],
  "mcp_servers": []
}
